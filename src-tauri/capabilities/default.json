{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for ComputerAgent.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
